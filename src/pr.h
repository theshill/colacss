// processor
#include <iostream>
#include <string>

using namespace std;

class Processor
{
    public:

    void process ( int argc, char** argv ) {
      cout << "Usage: cola styles.cola <options> or cola <command> <options>" << endl;
      cout << "Usage: cola styles.cola <options> or cola <command> <options>" << endl;
      cout << "Usage: cola styles.cola <options> or cola <command> <options>" << endl;
      cout << "Usage: cola styles.cola <options> or cola <command> <options>" << endl;
    }
};
